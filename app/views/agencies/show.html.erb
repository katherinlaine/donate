<div class="details">
  <h3><%= @agency.name %></h3>
  <%= render "agency_details", agency: @agency %>

  <h5><%= link_to "All Agencies", agencies_path %></h5>
</div>


<div id="map" data-markers='<%= @agency_markers.html_safe%>'></div>
